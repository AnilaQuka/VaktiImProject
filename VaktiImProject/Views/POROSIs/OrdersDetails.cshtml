@model List<VaktiImProject.Models.MyOrders>
@{
    ViewBag.Title = "Detaje Porosie";
    Layout = "~/Views/Shared/_LayoutPage3.cshtml";
}


<h3>Detajet e Porosisë</h3>

    <hr />
    @if (Model != null && Model.Count > 0)
    {
        foreach (var item in Model)
        {<div class="row">
                <div class=" col-md-12">
                    <br />
                    <br />
                    @*<p>--------------------------------------------------------------------------------------------------------</p>*@
                    <br />
                    <br />
                </div>
            </div>

            <div class="form-vertical">
                <div class="form-group">
                    @Html.LabelFor(modelItem => item.emriGatimit, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(modelItem => item.emriGatimit, new { htmlAttributes = new { @class = "form-control", @readonly = "true" } })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(modelIteme => item.sasia, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(modelItem => item.sasia, new { htmlAttributes = new { @class = "form-control", @readonly = "true" } })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(modelIteme => item.cmimi, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.EditorFor(modelItem => item.cmimi, new { htmlAttributes = new { @class = "form-control", @readonly = "true" } })
                    </div>
                </div>
                </div>
                

        }
    }
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <a href="@Url.Action("Myorders")" class="btn btn-info">
                <i class="fa fa-arrow-left"></i>
                Ktheu
            </a>
        </div>
    </div>

